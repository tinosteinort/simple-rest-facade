# language setting
language: java

# version numbers
jdk:
    - oraclejdk8

# pom.xml is not in Repository root
script:
    cd simple-rest-facade && mvn clean install && cd ../simple-rest-facade-json mvn clean install && cd ../simple-rest-facade-xml && cd ../simple-rest-facade-xml && mvn clean install && cd ../simple-rest-facade-test && mvn clean test install
