# language setting
language: java

# version numbers
jdk:
    - oraclejdk8

before_script:
    mkdir -p shippable/codecoverage

# pom.xml is not in Repository root
script:
    cd simple-rest-facade && mvn install -DskipTests=true

after_success:
    cp **/coverage.xml ../shippable/codecoverage/ && mvn test cobertura:cobertura
