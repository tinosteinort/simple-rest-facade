# language setting
language: java

# version numbers
jdk:
    - oraclejdk8

# pom.xml is not in Repository root
script:
    cd simple-rest-facade && mvn install -DskipTests=true

after_success:
    mvn test
